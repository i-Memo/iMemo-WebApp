import React, {useState} from 'react'

function OnScrollInput() {
    const [num, setnum] = useState(0)
    return (
        <React.Fragment>
            
        </React.Fragment>
    )
}

export default OnScrollInput
